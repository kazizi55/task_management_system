<h1>タスク一覧</h1>

<%= link_to '新規作成', "/tasks/new", method: :get %>

<% @tasks.each do |task| %>
    <h2><%= task.name %></h2>
    <p><%= task.explanation %></p>
    <p><%= link_to '詳細', "/tasks/#{task.id}", method: :get %></p>
<% end %>
