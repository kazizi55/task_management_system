<%= form_with(model: @label, local: true) do |f| %>
  <%= render 'layouts/error_messages', model: @label %>
  <%= f.label :name, '名前' %>
  <%= f.text_field :name, value: @label.name %>
  
  <%= f.submit '保存' %>
<% end %>
